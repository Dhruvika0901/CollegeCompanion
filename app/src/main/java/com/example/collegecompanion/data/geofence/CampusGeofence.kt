package com.example.collegecompanion.data.geofence

object CampusGeofence {
    const val GEOFENCE_ID    = "COLLEGE_CAMPUS"
    const val DEFAULT_RADIUS = 200f   // used as the UI default only — no hardcoded lat/lng
}